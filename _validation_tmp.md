# VALIDATION — ui-ux-plan (PART-01.draft.md vs templates/PART-01.md, 2026-09-04)
1. §B last fact line (Windows high-contrast detection method) has no verification date and is marked UNVERIFIED; RECON-CHECKLIST.md marks that item [x] while its text still says "NOT YET DEFINED" — the detection method must be owner-defined before gate g2 → run owner-resolve ui-ux-plan
2. OWNER ANSWERS Q5 (enforce root.minsize(820, 600)) is accepted but not integrated anywhere in §A/§C (§C still says only "resizable with remembered size in cfg") → fix in the draft, then re-validate
3. OWNER ANSWERS Q6 (document the file:// repo-root assumption in UserGuide.txt) is accepted but §C's UserGuide.txt entry mentions only the theme-toggle section → fix in the draft, then re-validate
4. OWNER ANSWERS Q7 (skip custom colors when high-contrast detection fails or is ambiguous) is accepted but §G covers only "high-contrast is active", not the detection-failure fallback → fix in the draft, then re-validate
