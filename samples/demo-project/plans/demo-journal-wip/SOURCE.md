PROJECT BRIEF — pasted into the Plan Console Intake tab on 2026-08-25

I write a daily note in journal/YYYY-MM-DD.md. I want a tiny helper
CLI, "jr", that can:
- today: create (or open) today's file with a date heading
- grep: search all entries for a word, print matching dates
- stats: count entries per month

Standard library only. The journal folder is sacred — never edit old
entries.

(This example is intentionally left MID-WORKFLOW: the draft still has
an open question and a parked owner answer, one recon item is unchecked
and one is DEFERRED — exactly what you see in the Owner pass tab before
Freeze is possible.)
